name: RSS Fetcher Cron

# Ütemezés
on:
  schedule:
    - cron: '0 * * * *'  # minden órában
  workflow_dispatch:      # manuális indítás lehetősége

jobs:
  fetch-rss:
    runs-on: ubuntu-latest
    steps:
      - name: Call Supabase Edge Function
        run: |
          curl -X GET https://lcqvsevemstgtdncqoki.supabase.co/functions/v1/rss_fetcher
